# ============================================================
#  BunnyBot — Python dependencies
#  No AI, no internet, pure local OpenCV vision
# ============================================================

# ── Install via pkg (Termux) — NOT pip ──────────────────────
#
#   OpenCV and NumPy MUST come from Termux's package manager.
#   pip install opencv-python will hang trying to compile C++.
#
#     pkg install python-numpy opencv-python -y
#
# ── The ONLY pip package needed ─────────────────────────────
#
#   (nothing — this bot has zero pip dependencies)
#
# ── Full Termux setup (copy-paste, run once) ─────────────────
#
#   pkg update && pkg upgrade -y
#   pkg install python android-tools python-numpy opencv-python git -y
#   python bunny_bot.py
#
# ── ADB (already included above) ────────────────────────────
#
#   pkg install android-tools
#
# ============================================================
