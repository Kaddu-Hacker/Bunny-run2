# ============================================================
#  BunnyBot — Python Dependencies
#  Compatible with Termux (Android) and standard Linux/Mac/Windows
# ============================================================

# ── Install via pip (works natively on Termux) ──────────────
google-generativeai>=0.5.0

# ── Optional: Faster image handling for AI (install if available) ──
# pip install Pillow
# Pillow is optional — the bot will work without it (falls back to base64 encoding)

# ── OpenCV + NumPy: MUST be installed via pkg in Termux ─────
#
#   DO NOT run:  pip install opencv-python   ← will try to compile C++, will hang!
#
#   Instead run these ONE TIME in Termux:
#     pkg install python-numpy
#     pkg install opencv-python
#
#   These provide both numpy and cv2 without any compilation.
#   The pixel-reflex feature (fast obstacle detection) requires these.
#   If not installed, the bot will still work using AI-only mode.
#
# ── ADB (Android Debug Bridge) ──────────────────────────────
#
#   In Termux, ADB is installed via:
#     pkg install android-tools
#
# ============================================================
