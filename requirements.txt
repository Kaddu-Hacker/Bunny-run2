# ============================================================
#  BunnyBot — Python Dependencies
#  ✅ Termux-safe: all install via pip with NO Rust compilation
# ============================================================

# Core HTTP client — used for ALL AI API calls (OpenRouter + Google)
# Pure Python, no Rust, installs instantly on Termux
requests>=2.28.0

# ── Optional: faster image handling ─────────────────────────
# pip install Pillow
# Not required — images are already sent as base64 via requests

# ── OpenCV + NumPy: install via pkg (NOT pip) in Termux ─────
#   ⚠️  DO NOT run: pip install opencv-python
#       It compiles C++ and hangs on Termux!
#
#   Run these ONCE in Termux instead:
#     pkg install python-numpy
#     pkg install opencv-python
#
#   These enable pixel-reflex fast obstacle detection.
#   Without them, bot runs in AI-only mode (still works fine).
#
# ── ADB ──────────────────────────────────────────────────────
#   pkg install android-tools
#
# ── Full Termux quick-start ──────────────────────────────────
#   pkg update && pkg upgrade -y
#   pkg install python android-tools python-numpy opencv-python -y
#   pip install requests
#   python bunny_bot.py
# ============================================================
